# Geopolymer Clogging Prediction – ML App

A Streamlit-based machine learning application to predict:

- **Clogging Rate (% per year)**

## Models Used
- Linear Regression (LR)
- Support Vector Regression (SVR)
- XGBoost (XGB)
- Bayesian Model Averaging (Bayesian Ridge)

## How to Run (Local)
1. pip install -r requirements.txt
2. streamlit run app.py

## Deployment
The app is deployed on Streamlit Cloud using a GitHub repository.
